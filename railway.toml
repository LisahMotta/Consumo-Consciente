[build]
builder = "nixpacks"
buildCommand = "rm -rf app/node_modules app/package-lock.json && cd app && npm install && npm run build"

[deploy]
startCommand = "npx serve app/dist --listen $PORT"
restartPolicyType = "on_failure"
