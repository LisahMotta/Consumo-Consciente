{
  "name": "consumo-consciente-mvp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "npx serve app/dist --listen $PORT"
  }
}
